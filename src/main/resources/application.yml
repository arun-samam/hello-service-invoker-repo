spring:
  application:
    name: hello-service-invoker
  config:
    import: optional:configserver:http://config-server:8888
  profiles:
    active: default
server:
  port: 4568
  address: 0.0.0.0
hello:
  service:
    url: ${HELLO_SERVICE_URL:http://localhost:4567}